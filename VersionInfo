MAJOR_VERSION=0
MINOR_VERSION=6
PATCH_LEVEL=0
PKG_NAME=SWinTypeLibs

TAR_FILE=${PKG_NAME}_${MAJOR_VERSION}.${MINOR_VERSION}-${PATCH_LEVEL}.tar.gz
TAR_SRC_FILE=${TAR_FILE}
ZIP_FILE=${PKG_NAME}_${MAJOR_VERSION}.${MINOR_VERSION}-${PATCH_LEVEL}.zip

#ZIP_SRC_FILE=${PKG_NAME}_${MAJOR_VERSION}.${MINOR_VERSION}-${PATCH_LEVEL}.zip
