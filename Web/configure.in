AC_INIT(index.html.in)

. ../VersionInfo

DATE=`date +'%Y/%m/%d'`

AC_SUBST(DATE)

AC_SUBST(TAR_FILE)
AC_SUBST(MAJOR_VERSION)
AC_SUBST(MINOR_VERSION)
AC_SUBST(PATCH_LEVEL)
AC_SUBST(PKG_NAME)

AC_SUBST(ZIP_SRC_FILE)
AC_SUBST(ZIP_FILE)

AC_SUBST(TAR_SRC_FILE)

AC_OUTPUT(index.html)
